# Copy to .env and adjust. NEVER commit the real .env.
DB_USER=shop
DB_PASSWORD=change-me-in-prod
# Used by the app container (points at the compose "db" service, in-network port 5432):
DB_URL=jdbc:postgresql://db:5432/shop
